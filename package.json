{
  "name": "cly-research-core",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first research core for sources, experiments, provenance, and claims.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
